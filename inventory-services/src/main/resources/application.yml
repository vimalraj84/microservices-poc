server:
  port: 9090
  servlet:
    context-path: /intvSvc

spring:
  banner:
    location: bilqu-banner.txt

  jpa:
    show-sql: true

  h2:
    console:
      enabled: true

eureka:
  client:
    service-url:
      default-zone : http://localhost:8761/eureka